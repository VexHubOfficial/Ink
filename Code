--// MALEVOLENT HUB
--// LocalScript
--// Place in StarterPlayerScripts or StarterGui

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")

local LocalPlayer = Players.LocalPlayer

--==================================================
-- CONFIG
--==================================================

local MAIN_SIZE = UDim2.fromOffset(520, 320)

local RED = Color3.fromRGB(210, 25, 25)
local DARK_RED = Color3.fromRGB(55, 8, 8)
local BG = Color3.fromRGB(13, 13, 16)
local ELEMENT = Color3.fromRGB(25, 25, 29)
local ELEMENT_HOVER = Color3.fromRGB(55, 12, 12)

--==================================================
-- GUI
--==================================================

local Gui = Instance.new("ScreenGui")
Gui.Name = "MalevolentHub"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

--==================================================
-- MAIN
--==================================================

local Main = Instance.new("Frame")
Main.Name = "Main"
Main.Size = MAIN_SIZE
Main.Position = UDim2.new(0.5, -260, 0.5, -160)
Main.BackgroundColor3 = BG
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.Parent = Gui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = RED
MainStroke.Thickness = 2
MainStroke.Transparency = 0.15
MainStroke.Parent = Main

--==================================================
-- HEADER
--==================================================

local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 52)
Header.BackgroundColor3 = DARK_RED
Header.BorderSizePixel = 0
Header.Parent = Main

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 12)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.BackgroundTransparency = 1
Title.Position = UDim2.fromOffset(15, 0)
Title.Size = UDim2.new(1, -120, 1, 0)
Title.Text = "MALEVOLENT HUB"
Title.TextColor3 = RED
Title.TextSize = 20
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header

--==================================================
-- LOCK BUTTON
--==================================================

local LockButton = Instance.new("TextButton")
LockButton.Name = "LockButton"
LockButton.Size = UDim2.fromOffset(75, 32)
LockButton.Position = UDim2.new(1, -85, 0, 10)
LockButton.BackgroundColor3 = ELEMENT
LockButton.BorderSizePixel = 0
LockButton.Text = "🔓"
LockButton.TextColor3 = Color3.fromRGB(255, 255, 255)
LockButton.TextSize = 17
LockButton.Font = Enum.Font.GothamBold
LockButton.AutoButtonColor = false
LockButton.Parent = Header

local LockCorner = Instance.new("UICorner")
LockCorner.CornerRadius = UDim.new(0, 8)
LockCorner.Parent = LockButton

local Locked = false

LockButton.MouseButton1Click:Connect(function()
	Locked = not Locked

	if Locked then
		LockButton.Text = "🔒"
		LockButton.BackgroundColor3 = DARK_RED
	else
		LockButton.Text = "🔓"
		LockButton.BackgroundColor3 = ELEMENT
	end
end)

--==================================================
-- SCROLLING CONTENT
--==================================================

local Content = Instance.new("ScrollingFrame")
Content.Name = "Content"
Content.Position = UDim2.fromOffset(12, 62)
Content.Size = UDim2.new(1, -24, 1, -74)
Content.BackgroundTransparency = 1
Content.BorderSizePixel = 0
Content.ScrollBarThickness = 5
Content.ScrollBarImageColor3 = RED
Content.ScrollingDirection = Enum.ScrollingDirection.Y
Content.CanvasSize = UDim2.new(0, 0, 0, 0)
Content.AutomaticCanvasSize = Enum.AutomaticSize.Y
Content.Parent = Main

local Layout = Instance.new("UIListLayout")
Layout.Padding = UDim.new(0, 8)
Layout.SortOrder = Enum.SortOrder.LayoutOrder
Layout.Parent = Content

local Padding = Instance.new("UIPadding")
Padding.PaddingBottom = UDim.new(0, 10)
Padding.Parent = Content

--==================================================
-- UI HELPERS
--==================================================

local function Section(text)

	local Label = Instance.new("TextLabel")
	Label.Size = UDim2.new(1, -5, 0, 28)
	Label.BackgroundTransparency = 1
	Label.Text = text
	Label.TextColor3 = RED
	Label.TextSize = 16
	Label.Font = Enum.Font.GothamBold
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.Parent = Content

	return Label
end

local function CreateTextBox(placeholder)

	local Box = Instance.new("TextBox")

	Box.Size = UDim2.new(1, -5, 0, 38)
	Box.BackgroundColor3 = ELEMENT
	Box.BorderSizePixel = 0
	Box.PlaceholderText = placeholder
	Box.PlaceholderColor3 = Color3.fromRGB(120, 120, 125)
	Box.Text = ""
	Box.TextColor3 = Color3.fromRGB(245, 245, 245)
	Box.TextSize = 14
	Box.Font = Enum.Font.Gotham
	Box.ClearTextOnFocus = false
	Box.Parent = Content

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Box

	local Stroke = Instance.new("UIStroke")
	Stroke.Color = Color3.fromRGB(70, 25, 25)
	Stroke.Thickness = 1
	Stroke.Parent = Box

	return Box
end

local function CreateButton(text)

	local Button = Instance.new("TextButton")

	Button.Size = UDim2.new(1, -5, 0, 38)
	Button.BackgroundColor3 = DARK_RED
	Button.BorderSizePixel = 0
	Button.Text = text
	Button.TextColor3 = Color3.fromRGB(255, 255, 255)
	Button.TextSize = 14
	Button.Font = Enum.Font.GothamBold
	Button.AutoButtonColor = false
	Button.Parent = Content

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Button

	Button.MouseEnter:Connect(function()
		TweenService:Create(
			Button,
			TweenInfo.new(0.12),
			{BackgroundColor3 = ELEMENT_HOVER}
		):Play()
	end)

	Button.MouseLeave:Connect(function()
		TweenService:Create(
			Button,
			TweenInfo.new(0.12),
			{BackgroundColor3 = DARK_RED}
		):Play()
	end)

	return Button
end

local function CreateToggle(text)

	local Button = Instance.new("TextButton")

	Button.Size = UDim2.new(1, -5, 0, 38)
	Button.BackgroundColor3 = ELEMENT
	Button.BorderSizePixel = 0
	Button.Text = text .. "  [ OFF ]"
	Button.TextColor3 = Color3.fromRGB(245, 245, 245)
	Button.TextSize = 14
	Button.Font = Enum.Font.GothamBold
	Button.AutoButtonColor = false
	Button.Parent = Content

	local Corner = Instance.new("UICorner")
	Corner.CornerRadius = UDim.new(0, 8)
	Corner.Parent = Button

	local Enabled = false

	Button.MouseButton1Click:Connect(function()

		Enabled = not Enabled

		if Enabled then

			Button.Text = text .. "  [ ON ]"
			Button.BackgroundColor3 = RED

		else

			Button.Text = text .. "  [ OFF ]"
			Button.BackgroundColor3 = ELEMENT

		end
	end)

	return Button, function()
		return Enabled
	end
end

--==================================================
-- MOVEMENT SECTION
--==================================================

Section("🚀 MOVEMENT")

local PlayerBox = CreateTextBox("Display name prefix...")

local ConstantTPButton, IsConstantTP =
	CreateToggle("Constant TP")

-- Case-insensitive prefix matching
local function FindPlayer(prefix)

	if prefix == "" then
		return nil
	end

	local wanted = string.lower(prefix)

	for _, Player in ipairs(Players:GetPlayers()) do

		if Player ~= LocalPlayer then

			local DisplayName =
				string.lower(Player.DisplayName)

			if string.sub(DisplayName, 1, #wanted) == wanted then
				return Player
			end

		end
	end

	return nil
end

RunService.Heartbeat:Connect(function()

	if not IsConstantTP() then
		return
	end

	local Target = FindPlayer(PlayerBox.Text)

	if not Target then
		return
	end

	local TargetCharacter = Target.Character
	local MyCharacter = LocalPlayer.Character

	if not TargetCharacter or not MyCharacter then
		return
	end

	local TargetRoot =
		TargetCharacter:FindFirstChild("HumanoidRootPart")

	local MyRoot =
		MyCharacter:FindFirstChild("HumanoidRootPart")

	if TargetRoot and MyRoot then

		MyRoot.CFrame =
			TargetRoot.CFrame + Vector3.new(0, 100, 0)

	end
end)

--==================================================
-- PARTS SECTION
--==================================================

Section("🗑️ PARTS")

local PartBox = CreateTextBox("Part name...")

local DeletePartButton =
	CreateButton("DELETE PART")

local TPPartButton =
	CreateButton("TP TO PART")

local function FindPart(name)

	if name == "" then
		return nil
	end

	local wanted = string.lower(name)

	for _, Object in ipairs(workspace:GetDescendants()) do

		if Object:IsA("BasePart") then

			if string.lower(Object.Name) == wanted then
				return Object
			end

		end
	end

	return nil
end

-- Client-side visual deletion
DeletePartButton.MouseButton1Click:Connect(function()

	local Part = FindPart(PartBox.Text)

	if not Part then
		return
	end

	Part.LocalTransparencyModifier = 1
	Part.CanCollide = false
end)

-- TP to part
TPPartButton.MouseButton1Click:Connect(function()

	local Part = FindPart(PartBox.Text)

	if not Part then
		return
	end

	local Character = LocalPlayer.Character

	if not Character then
		return
	end

	local Root =
		Character:FindFirstChild("HumanoidRootPart")

	if Root then
		Root.CFrame =
			Part.CFrame + Vector3.new(0, 4, 0)
	end
end)

--==================================================
-- COLLAPSE BUTTON
--==================================================

local CollapseButton = Instance.new("TextButton")

CollapseButton.Name = "CollapseButton"
CollapseButton.Size = UDim2.fromOffset(55, 55)
CollapseButton.Position = UDim2.new(0, 15, 0.5, -27)
CollapseButton.BackgroundColor3 = DARK_RED
CollapseButton.BorderSizePixel = 0
CollapseButton.Text = "M"
CollapseButton.TextColor3 = RED
CollapseButton.TextSize = 24
CollapseButton.Font = Enum.Font.GothamBold
CollapseButton.AutoButtonColor = false
CollapseButton.Parent = Gui

local CollapseCorner = Instance.new("UICorner")
CollapseCorner.CornerRadius = UDim.new(1, 0)
CollapseCorner.Parent = CollapseButton

local CollapseStroke = Instance.new("UIStroke")
CollapseStroke.Color = RED
CollapseStroke.Thickness = 2
CollapseStroke.Parent = CollapseButton

local Open = true

CollapseButton.MouseButton1Click:Connect(function()

	Open = not Open

	Main.Visible = Open

	if Open then
		CollapseButton.Text = "M"
	else
		CollapseButton.Text = "+"
	end
end)

--==================================================
-- DRAGGING
--==================================================

local function MakeDraggable(Object, CanDrag)

	local Dragging = false
	local DragStart
	local StartPosition

	Object.InputBegan:Connect(function(Input)

		if Input.UserInputType ~= Enum.UserInputType.MouseButton1
			and Input.UserInputType ~= Enum.UserInputType.Touch then
			return
		end

		if CanDrag and not CanDrag() then
			return
		end

		Dragging = true
		DragStart = Input.Position
		StartPosition = Object.Position

		Input.Changed:Connect(function()

			if Input.UserInputState == Enum.UserInputState.End then
				Dragging = false
			end

		end)
	end)

	UserInputService.InputChanged:Connect(function(Input)

		if not Dragging then
			return
		end

		if Input.UserInputType ~= Enum.UserInputType.MouseMovement
			and Input.UserInputType ~= Enum.UserInputType.Touch then
			return
		end

		local Delta =
			Input.Position - DragStart

		Object.Position = UDim2.new(
			StartPosition.X.Scale,
			StartPosition.X.Offset + Delta.X,

			StartPosition.Y.Scale,
			StartPosition.Y.Offset + Delta.Y
		)
	end)
end

-- Main GUI respects lock
MakeDraggable(Main, function()
	return not Locked
end)

-- Collapse button is ALWAYS draggable
MakeDraggable(CollapseButton, function()
	return true
end)

print("Malevolent Hub loaded successfully.")
